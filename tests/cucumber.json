{
  "default": {
    "require": ["bdd/steps/**/*.ts"],
    "requireModule": ["ts-node/register"],
    "paths": ["bdd/features/**/*.feature"],
    "format": ["progress", "html:bdd/report/index.html"],
    "formatOptions": { "snippetInterface": "async-await" },
    "publishQuiet": true
  }
}
