{
  "default": {
    "require": ["tests/bdd/steps/**/*.ts"],
    "requireModule": ["ts-node/register"],
    "paths": ["tests/bdd/features/**/*.feature"],
    "format": ["progress-bar", "html:tests/bdd/report/index.html"],
    "formatOptions": { "snippetInterface": "async-await" },
    "publishQuiet": true
  }
}
